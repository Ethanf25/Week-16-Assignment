// src/data/testData.ts

export interface Task {
  id: number;
  name: string; // Add this line for the name property
  completed: boolean;
}

// Example test data:
export const testData: Task[] = [ //this shows the test data for the tasks array in the App component
  
];
